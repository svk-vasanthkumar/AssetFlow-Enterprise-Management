// Re-export PageLoader as default so existing imports resolve correctly.
// The actual component lives in Loader.jsx as a named export.
import { PageLoader } from './Loader';
export default PageLoader;
